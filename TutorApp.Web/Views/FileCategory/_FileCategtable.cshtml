@using TutorApp.Web.Helper;
@model TutorApp.Web.ViewModels.FileCategSearchViewModel
@{

    var i = 0;
}

<table>
    <thead>
        <tr>
            <th>S.No</th>

            <th>Name</th>
            <th>Description</th>
            <th>Action</th>

        </tr>
    </thead>
    <tbody>
        @foreach (var categ in Model.FileCateg)
        {
            i++;
            <tr>
                <td>@i</td>
                <td>@categ.Name</td>
                <td>@categ.Description</td>
                <td>
                    <button class="editbtn fas fa-edit" data-id="@categ.ID"></button>
                    <button class="deletebtn fas fa-trash-alt" data-id="@categ.ID"></button>
                </td>
            </tr>
        }
    </tbody>
</table>
<div class="pager">
    @if (Model.Pager != null)
    {

        if (Model.Pager.CurrentPage > 1)
        {
            <a class="Pagebtn fas fa-angle-double-left" data-pageNo="1"></a>
            <a class="Pagebtn fas fa-angle-left" data-pageNo="@(Model.Pager.CurrentPage -1)"></a>
        }

        for (var page = Model.Pager.StartPage; page <= Model.Pager.EndPage; page++)
        {
            string activeclass = Model.Pager.CurrentPage == page ? "active " : string.Empty;
            <a class="Pagebtn" data-pageNo="@page">@page</a>

        }

        if (Model.Pager.CurrentPage < Model.Pager.TotalPages)
        {
            <a class="Pagebtn fas fa-angle-right" data-pageNo="@(Model.Pager.CurrentPage+1)"></a>
            <a class="Pagebtn fas fa-angle-double-right" data-pageNo="@Model.Pager.EndPage"></a>
        }
    }
</div>

<script>

    $(".Pagebtn").click(function() {
        $.ajax({
                url: "@Url.FileCategoryTable("")",
        data:
                {
                pageNo: $(this).attr("data-pageNo")
        }
            })
        .done(function(response) {
            $("#tablecontainer").html(response);
            })
        .fail(function(XMLHttpRequest, status, errorthrown) { alert("error"); });
});



        $(".editbtn").click(function () {

            $.ajax({
                url: "@Url.EditFileCategory()",
                data: {
                    ID: $(this).attr("data-id")
                }
            })
                .done(function (response) {
                    $("#actioncontainer").html("");
                    $("#actioncontainer").html(response);
                })
                .fail(function (XMLHttpRequest, status, errorthrown) { alert("error"); });
            $([document.documentElement, document.body]).animate({
                scrollTop: $("#actioncontainer").offset().top
            }, 1000);
        });

        $(".deletebtn").click(function () {
            var confirmation = confirm("Are you sure you want to delete");
            if (confirmation) {

                $.ajax({
                    type: "POST",
                    url: "@Url.DeleteFileCategory()",
                    data: {
                        ID: $(this).attr("data-id")
                    }
                })
                    .done(function (response) {
                        $("#tablecontainer").html(response);
                    })
                    .fail(function (XMLHttpRequest, status, errorthrown) { alert("error"); });
            }
        });

</script>














